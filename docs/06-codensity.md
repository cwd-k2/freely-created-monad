# 補遺: Codensity モナド

Freer モナドの `>>=` は左結合チェーンで O(n²) になり得ます。この性能問題がどこから来るのか、そして **Codensity モナド** がどう解決するのかを見ていきます。

## Codensity モナド

Freer モナドの `>>=` は継続を関数合成で連鎖させます。

```haskell
Bind fx k >>= f = Bind fx ((>>= f) . k)
```

左結合の `>>=` チェーンでは継続が入れ子になります。具体的に追ってみましょう。

```
m >>= f                  = Bind fx ((>>= f) . k)
(m >>= f) >>= g          = Bind fx ((>>= g) . (>>= f) . k)
((m >>= f) >>= g) >>= h  = Bind fx ((>>= h) . (>>= g) . (>>= f) . k)
```

`>>=` のたびに継続の先頭に関数合成が一段追加されます。実行時に `k` を呼ぶと、合成された関数を左から順に剥がしていく必要があり、n 回の `>>=` に対して `1 + 2 + ... + n = O(n²)` のコストがかかります。

**Codensity モナド** はこの問題を解決します。

```haskell
newtype Codensity m a = Codensity
  { runCodensity :: forall r. (a -> m r) -> m r }
```

これは「CPS 変換されたモナド」です。Monad インスタンスは：

```haskell
instance Monad (Codensity m) where
  Codensity m >>= f = Codensity (\k -> m (\a -> runCodensity (f a) k))
```

`>>=` が常に **O(1)** になります。継続の結合方向が右結合に矯正されるためです。

リストの `(++)` が左結合で O(n²) になるのを差分リスト `[a] -> [a]` で O(n) にするのと同じ発想です。

## パラメトリシティ

Codensity の型をもう一度見てみましょう。

```haskell
forall r. (a -> m r) -> m r
```

`forall r.` は「**任意の** 型 `r` に対して動作する」という意味です。パラメトリシティ（parametricity）により、この型の値 `m` は `r` について**何も知ることができません**。

具体的には、`m` にできることは：

1. 手元の `a` 型の値を継続 `k : a -> m r` に渡して `m r` を得る
2. 得られた `m r` をそのまま返す

`r` が何であるかによって振る舞いを変えることは不可能です。`m r` の中身を調べることも、`r` 型の値を独自に構築することもできません。

> **注**: Freer モナドの文脈では、`k` を使わずに `Freer f r` を構築するには `Pure` に `r` 型の値を渡す必要がありますが、`r` は任意の型なのでそのような値を持てません。結局 `k` を呼ぶ以外に選択肢はありません。

実際に、Codensity の `>>=` が結合律を満たすことは直接計算で確認できます。

```
(Codensity m >>= f) >>= g
= Codensity (\k -> m (\a -> runCodensity (f a) (\b -> runCodensity (g b) k)))

Codensity m >>= (\a -> f a >>= g)
= Codensity (\k -> m (\a -> runCodensity (f a >>= g) k))
= Codensity (\k -> m (\a -> runCodensity (f a) (\b -> runCodensity (g b) k)))
```

両辺が一致します。この計算が成り立つ背景には**パラメトリシティ**があります。`forall r.` により `Codensity m a` の値は `r` について何も知ることができず、継続を「呼ぶ以外に何もできない」ため、継続の合成順序を自由に組み替えても観測可能な差が生じません。

これは差分リストの場合と同様です。`\xs -> [1,2,3] ++ xs` という関数に対して、`xs` の具体的な中身は見えません。だから `(f . g) . h` と `f . (g . h)` を区別する方法がなく、関数合成の結合律がそのままリストの `(++)` の結合律を保証します。

## Freer との接続

Freer の `>>=` が左結合で O(n²) になる問題は、プログラムの**構築時**に `Codensity (Freer f)` を使い、**実行時**に `lowerCodensity` で戻すことで解決できます。

```haskell
liftCodensity :: Monad m => m a -> Codensity m a
liftCodensity m = Codensity (m >>=)

lowerCodensity :: Monad m => Codensity m a -> m a
lowerCodensity (Codensity f) = f pure
```

do 記法は `>>=` を**右結合にデシュガー**するため、do 記法でプログラムを組み立てる限りこの問題は発生しません。左結合チェーンが現れるのは、do 記法の外でプログラム的に `>>=` を反復適用する場合（例: `foldl (>>=) m fs`）に限られます。本資料の DSL 実装は do 記法（TypeScript では Generator）のみを使うため、Codensity による最適化は行いません。

## Generator と Codensity の関係

Codensity の `>>=` による再結合は各継続を1回しか呼ばないため、one-shot 制約とは矛盾しません。Generator に Codensity が適用できない理由は別のところにあります。

1. **再結合の対象がない** — Codensity は `>>=` チェーンのデータ構造を再結合（右結合化）することで性能を改善します。ところが Generator では bind（`yield*`）が言語ランタイムに暗黙に管理されており、プログラムからアクセスできる結合構造が存在しません
2. **そもそも左結合問題が発生しない** — Generator の `yield*` はサブジェネレータへの委譲であり、本質的に右結合です。Freer で問題になるような左結合の `>>=` チェーン（`foldl (>>=) m fs`）に相当する構造が Generator では自然に生じません

つまり、Generator では Codensity が解決すべき問題自体が存在せず、仮に問題があったとしても再結合を適用する手段がない、という二重の意味で Codensity は無関係です。

> 📖 対応コード: [`haskell/src/Codensity.hs`](../haskell/src/Codensity.hs)
